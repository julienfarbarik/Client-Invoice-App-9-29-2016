##### app.js notes:

variables:
  HOST_PORT  (line 2) sets the port the application will connect to (see server.js for server-side variables)
  
  $scope variables:
    dynamic variables associated with form data, should not be changed unless also modified on static>app.html
